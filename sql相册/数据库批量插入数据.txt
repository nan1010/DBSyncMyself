use test_dev;
DROP PROCEDURE IF EXISTS proc_initData;
DELIMITER $
CREATE PROCEDURE proc_initData()
BEGIN
    DECLARE i INT DEFAULT 1;
    WHILE i<=10000 DO
        INSERT INTO t_user(username) VALUES(i);
        SET i = i+1;
    END WHILE;
END $
CALL proc_initData();